program MeuCod
	var v1, v2 : integer;
 /* eu 
amo bolinhos */
	procedure Soma (cont, num : real; total : real) 
	var v1 : real;
	{var Soma : real;}
	begin
		read (v1);
		bolo := 2 + 3.5;
		bolacha(recheio;massa);
		write (v2);  { bolinhos sao ops}
		while cont < total do write (num) $
	end
	begin
	if v1 <= v2 then 
		read (v1) 
	else 
		read (v2) $ 
	end.
{Começar a fazer a analise semantica da linguagem. emplementar a pilha de escopo. tirar variavel valor da tabela de simbolos}